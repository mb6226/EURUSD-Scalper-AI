# EURUSD M1 ZigZag Quarter Stability

Candidates are inherited from the previous optimization step: results/zigzag_monthly/quarterly_candidates.csv.
Each period is evaluated with historical warmup so ZigZag state is not reset at the boundary.
Outputs contain per-period results plus cross-period stability rankings.
